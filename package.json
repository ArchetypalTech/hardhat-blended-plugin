{
  "name": "@fluentxyz/hardhat-compile-to-wasm",
  "version": "0.1.0",
  "description": "A Hardhat plugin to compile Rust contracts to WebAssembly (WASM) and generate Hardhat artifacts.",
  "repository": "github:fluentlabs-xyz/hardhat-compile-to-wasm",
  "author": "Fluent labs",
  "license": "MIT",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "keywords": [
    "ethereum",
    "smart-contracts",
    "hardhat",
    "hardhat-plugin",
    "fluent",
    "wasm",
    "webassembly",
    "compile"
  ],
  "scripts": {
    "lint:fix": "prettier --write 'src/**/*.{js,ts}' 'test/**/*.{js,ts}' && tslint --fix --config tslint.json --project tsconfig.json",
    "lint": "tslint --config tslint.json --project tsconfig.json",
    "test": "mocha --config mocha.config.js",
    "build": "tsc",
    "watch": "tsc -w",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/src/",
    "src/",
    "LICENSE",
    "README.md"
  ],
  "devDependencies": {
    "@types/chai": "^4.3.16",
    "@types/fs-extra": "^5.0.4",
    "@types/mocha": "^5.2.7",
    "@types/node": "^8.10.38",
    "@types/sinon": "^17.0.3",
    "@types/sinon-chai": "^3.2.12",
    "chai": "^4.2.0",
    "hardhat": "^2.0.0",
    "mocha": "^10.4.0",
    "prettier": "2.0.5",
    "sinon": "^18.0.0",
    "sinon-chai": "^3.7.0",
    "ts-node": "^8.10.2",
    "tslint": "^5.16.0",
    "tslint-config-prettier": "^1.18.0",
    "tslint-plugin-prettier": "^2.0.1",
    "typescript": "^4.0.3"
  },
  "peerDependencies": {
    "hardhat": "^2.0.0"
  }
}
